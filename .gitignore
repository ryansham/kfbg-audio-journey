# Secrets & handoff docs (contain live tokens — never commit)
CC-CONTEXT-TRANSFER.md
.env
.env.*
*.secret

# macOS
.DS_Store

# Editor / OS junk
*~
.vscode/
Thumbs.db

# Sanity studio
studio/node_modules/
studio/dist/
studio/.sanity/
studio/backups/

# Analytics dashboard — 伺服器端憑證同快取，一律不進版本控制
# 🔴 本 repo 是 PUBLIC。service account key 一旦推上去等於把整個 GA 帳戶交出去。
analytics/config.php
analytics/*key*.json
analytics/*credential*.json
analytics/*service-account*.json
analytics/cache/

# 秘密連結 folder 名。repo 是公開的，網址一公開就等於冇保護。見 analytics/README.md「存取控制」
analytics/.secret-folder
